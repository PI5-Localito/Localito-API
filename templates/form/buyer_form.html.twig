{{ form_start(form) }}
<div class="field label border">
    {{ form_widget(form.userId) }}
    <label>Id</label>
</div>
<div class="field label border">
    {{ form_widget(form.state) }}
    <label>Estado</label>
</div>
{{ form_widget(form.submit) }}
{{ form_end(form) }}
